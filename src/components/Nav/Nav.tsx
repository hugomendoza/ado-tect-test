import { StyledNav } from './Nav.styles'
import { Button } from '../Button/Button'

export const Nav = () => {
  return (
    <StyledNav>
      <Button
        type='button'
        value='Logout'
      />
    </StyledNav>
  )
}
